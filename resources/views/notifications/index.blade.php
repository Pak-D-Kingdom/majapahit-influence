@extends($role === 'superadmin' ? 'superadmin.layouts.app' : 'kol.layouts.app')
@section('title', 'Notifikasi')
@section('page-title', 'Notifikasi')
@section('content')
<div class="mb-6 flex items-end justify-between gap-4"><div><p class="text-sm text-slate-500">Informasi terbaru terkait aktivitas akunmu.</p><h2 class="mt-1 text-2xl font-bold text-slate-950">Notifikasi</h2></div><form method="POST" action="{{ route($role.'.notifications.read-all') }}">@csrf<button class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-600">Tandai semua dibaca</button></form></div>@if(session('success'))<div class="mb-5 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif<div class="space-y-3">@forelse($notifications as $notification)<div class="flex items-start gap-4 rounded-2xl border {{ $notification->is_read ? 'border-slate-200 bg-white' : 'border-indigo-100 bg-indigo-50/40' }} p-5 shadow-sm"><span class="flex size-10 shrink-0 items-center justify-center rounded-xl bg-indigo-100 text-indigo-600"><i class="bi bi-bell"></i></span><div class="min-w-0 flex-1"><p class="font-semibold text-slate-800">{{ $notification->title }}</p><p class="mt-1 text-sm leading-6 text-slate-500">{{ $notification->body }}</p><p class="mt-2 text-xs text-slate-400">{{ $notification->created_at->format('d M Y, H:i') }}</p></div>@if(!$notification->is_read)<form method="POST" action="{{ route($role.'.notifications.read',$notification) }}">@csrf @method('PATCH')<button class="text-xs font-semibold text-indigo-600">Tandai dibaca</button></form>@endif</div>@empty<div class="rounded-2xl border border-dashed border-slate-300 bg-white py-20 text-center text-sm text-slate-400">Belum ada notifikasi.</div>@endforelse</div><div class="mt-5">{{ $notifications->links() }}</div>
@endsection
