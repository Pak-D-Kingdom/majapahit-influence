@extends('kol.layouts.app')
@section('title','Detail Komisi')
@section('page-title','Detail Komisi')
@section('content')<div class="mb-6"><a href="{{ route('kol.commissions.index') }}" class="text-sm font-semibold text-indigo-600">← Kembali ke komisi</a><h2 class="mt-3 text-2xl font-bold text-slate-950">Detail komisi</h2><p class="mt-1 text-sm text-slate-500">{{ $commission->endorsement->campaign->name }} · {{ $commission->endorsement->campaign->brand->name }}</p></div>@if(session('success'))<div class="mb-5 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('success') }}</div>@endif<div class="grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2"><div class="flex items-center justify-between"><h3 class="font-bold text-slate-950">Rincian perhitungan</h3><x-dashboard.status-badge :status="$commission->status"/></div><dl class="mt-6 grid gap-5 sm:grid-cols-3"><div><dt class="text-xs text-slate-400">Fee endorsement</dt><dd class="mt-1 font-semibold text-slate-700">Rp{{ number_format($commission->endorsement_fee,0,',','.') }}</dd></div><div><dt class="text-xs text-slate-400">Persentase komisi</dt><dd class="mt-1 font-semibold text-slate-700">{{ $commission->commission_pct }}%</dd></div><div><dt class="text-xs text-slate-400">Komisi kamu</dt><dd class="mt-1 text-lg font-bold text-emerald-600">Rp{{ number_format($commission->commission_amount,0,',','.') }}</dd></div></dl><div class="mt-6 rounded-xl bg-slate-50 p-4 text-sm text-slate-600">Komisi dihitung transparan dari fee endorsement dan persentase tier yang berlaku.</div></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold text-slate-950">Ajukan pencairan</h3>@if($commission->status === 'approved' && !$commission->approvals->contains('action','request'))<form method="POST" action="{{ route('kol.commissions.request-disbursement',$commission) }}" class="mt-5">@csrf<textarea name="notes" rows="3" placeholder="Catatan tambahan (opsional)" class="w-full rounded-xl border-slate-200 text-sm"></textarea><button class="mt-3 w-full rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">Ajukan pencairan</button></form>@elseif($commission->status === 'dicairkan')<p class="mt-5 text-sm text-emerald-600">Komisi telah dicairkan pada {{ $commission->disbursed_at?->format('d M Y') ?: '-' }}.</p>@else<p class="mt-5 text-sm leading-6 text-slate-500">Komisi dapat diajukan setelah disetujui oleh Superadmin.</p>@endif</section></div><section class="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold text-slate-950">Riwayat proses</h3><div class="mt-5 space-y-4">@forelse($commission->approvals as $approval)<div class="flex gap-3"><span class="mt-1 size-2 rounded-full bg-indigo-500"></span><div><p class="text-sm font-semibold text-slate-800">{{ str($approval->action)->title() }}</p><p class="text-xs text-slate-400">{{ $approval->created_at->format('d M Y H:i') }} · {{ $approval->performer?->name ?: 'Sistem' }}</p>@if($approval->notes)<p class="mt-1 text-sm text-slate-500">{{ $approval->notes }}</p>@endif</div></div>@empty<p class="text-sm text-slate-400">Belum ada riwayat proses.</p>@endforelse</div></section>@endsection
