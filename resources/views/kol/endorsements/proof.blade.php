@extends('kol.layouts.app')
@section('title', 'Upload Bukti Konten')
@section('page-title', 'Upload Bukti Konten')
@section('content')
<div class="mb-6"><a href="{{ route('kol.endorsements.show', $endorsement) }}" class="text-sm font-semibold text-indigo-600">← Kembali ke endorsement</a><h2 class="mt-3 text-2xl font-bold text-slate-950">Upload bukti konten</h2><p class="mt-1 text-sm text-slate-500">{{ $endorsement->campaign->name }} · {{ $endorsement->campaign->brand->name }}</p></div>
@if ($errors->any())<div class="mb-5 rounded-xl bg-rose-50 p-4 text-sm text-rose-700"><ul class="list-inside list-disc">@foreach ($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="POST" enctype="multipart/form-data" action="{{ route('kol.endorsements.proof.store', $endorsement) }}" class="max-w-2xl space-y-6">@csrf<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="grid gap-5 sm:grid-cols-2"><label class="text-sm font-medium text-slate-700">Tanggal posting<input type="date" name="posted_at" required class="mt-2 w-full rounded-xl border-slate-200 text-sm"></label><label class="text-sm font-medium text-slate-700">Link posting<input type="url" name="post_url" placeholder="https://..." class="mt-2 w-full rounded-xl border-slate-200 text-sm"></label><label class="text-sm font-medium text-slate-700 sm:col-span-2">Catatan<textarea name="notes" rows="4" placeholder="Tambahkan catatan untuk Admin..." class="mt-2 w-full rounded-xl border-slate-200 text-sm"></textarea></label><label class="text-sm font-medium text-slate-700 sm:col-span-2">File bukti konten <span class="font-normal text-slate-400">(1–5 file, JPG/PNG/PDF, maks. 5MB/file)</span><input type="file" name="files[]" multiple required accept=".jpg,.jpeg,.png,.pdf" class="mt-2 block w-full rounded-xl border border-dashed border-slate-300 p-3 text-sm"></label></div></div><div class="flex justify-end gap-3"><a href="{{ route('kol.endorsements.show', $endorsement) }}" class="rounded-xl px-4 py-2.5 text-sm font-semibold text-slate-500">Batal</a><button class="rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-semibold text-white">Kirim untuk review</button></div></form>
@endsection
