@extends('kol.layouts.app')
@section('title','Endorsement Saya')
@section('page-title','Endorsement Saya')
@section('content')<div class="mb-6"><p class="text-sm text-slate-500">Lihat semua peluang kolaborasi dan progress pekerjaanmu.</p><h2 class="mt-1 text-2xl font-bold tracking-tight text-slate-950">Endorsement Saya</h2></div><div class="mb-5 flex gap-1 overflow-x-auto rounded-xl border border-slate-200 bg-white p-1 shadow-sm"><a href="{{ route('kol.endorsements.index',['tab'=>'aktif']) }}" class="whitespace-nowrap rounded-lg px-4 py-2 text-sm font-semibold {{ $tab==='aktif' ? 'bg-slate-950 text-white' : 'text-slate-500 hover:bg-slate-50' }}">Aktif</a><a href="{{ route('kol.endorsements.index',['tab'=>'mendatang']) }}" class="whitespace-nowrap rounded-lg px-4 py-2 text-sm font-semibold {{ $tab==='mendatang' ? 'bg-slate-950 text-white' : 'text-slate-500 hover:bg-slate-50' }}">Mendatang</a><a href="{{ route('kol.endorsements.index',['tab'=>'riwayat']) }}" class="whitespace-nowrap rounded-lg px-4 py-2 text-sm font-semibold {{ $tab==='riwayat' ? 'bg-slate-950 text-white' : 'text-slate-500 hover:bg-slate-50' }}">Riwayat</a></div><div class="space-y-3">@forelse($endorsements as $endorsement)<a href="{{ route('kol.endorsements.show',$endorsement) }}" class="group block rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-indigo-200 hover:shadow-md"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div class="flex items-center gap-4"><div class="flex size-12 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600"><i class="bi bi-megaphone text-xl"></i></div><div><h3 class="font-bold text-slate-950 group-hover:text-indigo-600">{{ $endorsement->campaign->name }}</h3><p class="mt-1 text-sm text-slate-500">{{ $endorsement->campaign->brand->name }} · {{ str($endorsement->content_type)->replace('_',' ')->title() }}</p></div></div><div class="flex items-center gap-5 sm:text-right"><div><p class="text-xs text-slate-400">Deadline</p><p class="mt-1 text-sm font-semibold text-slate-700">{{ $endorsement->deadline->format('d M Y') }}</p></div><x-dashboard.status-badge :status="$endorsement->status"/><i class="bi bi-chevron-right text-slate-300"></i></div></div></a>@empty<div class="rounded-2xl border border-dashed border-slate-300 bg-white py-20 text-center text-sm text-slate-400"><i class="bi bi-briefcase mb-2 block text-3xl"></i>Belum ada endorsement pada tab ini.</div>@endforelse</div><div class="mt-5">{{ $endorsements->links() }}</div>@endsection
