@extends('kol.layouts.app')
@section('title', 'Detail Endorsement')
@section('page-title', 'Detail Endorsement')
@section('content')
<div class="mb-6"><a href="{{ route('kol.endorsements.index') }}" class="text-sm font-semibold text-indigo-600">← Kembali</a><h2 class="mt-3 text-2xl font-bold text-slate-950">{{ $endorsement->campaign->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $endorsement->campaign->brand->name }} · {{ str($endorsement->content_type)->replace('_',' ')->title() }}</p></div>
@if(in_array($endorsement->status, ['assigned','in_progress','content_rejected']))<div class="mb-5 rounded-2xl border border-indigo-100 bg-indigo-50 p-4"><p class="text-sm text-indigo-800">Sudah selesai membuat konten?</p><a href="{{ route('kol.endorsements.proof.create',$endorsement) }}" class="mt-2 inline-flex rounded-lg bg-indigo-600 px-3 py-2 text-xs font-semibold text-white">Upload bukti konten</a></div>@endif
<div class="grid gap-6 lg:grid-cols-[1.2fr_0.8fr]"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold">Brief dan detail pekerjaan</h3><dl class="mt-5 space-y-5 text-sm"><div><dt class="text-xs text-slate-400">Fee</dt><dd class="mt-1 text-xl font-bold text-emerald-600">Rp{{ number_format($endorsement->fee,0,',','.') }}</dd></div><div><dt class="text-xs text-slate-400">Periode</dt><dd class="mt-1 text-slate-700">{{ $endorsement->start_date?->format('d M Y') ?: '-' }} — {{ $endorsement->deadline->format('d M Y') }}</dd></div><div><dt class="text-xs text-slate-400">Content requirement</dt><dd class="mt-1 whitespace-pre-line leading-6 text-slate-600">{{ $endorsement->campaign->content_requirements ?: '-' }}</dd></div><div><dt class="text-xs text-slate-400">Do & don'ts</dt><dd class="mt-1 whitespace-pre-line leading-6 text-slate-600">{{ $endorsement->campaign->dos_and_donts ?: '-' }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold">Content proof</h3>@forelse($endorsement->contentProofs as $proof)<div class="mt-4 rounded-xl bg-slate-50 p-4"><x-dashboard.status-badge :status="$proof->review_status"/>@if($proof->post_url)<a href="{{ $proof->post_url }}" target="_blank" rel="noopener" class="mt-3 block truncate text-sm font-semibold text-indigo-600">{{ $proof->post_url }}</a>@endif@if($proof->review_notes)<p class="mt-2 text-xs text-rose-600">{{ $proof->review_notes }}</p>@endif</div>@empty<p class="mt-5 text-sm text-slate-400">Belum ada content proof.</p>@endforelse</section></div>
@endsection
