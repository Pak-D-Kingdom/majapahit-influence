@extends('superadmin.layouts.app')
@section('title',$campaign->name)
@section('page-title','Detail Campaign')
@section('content')<div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('superadmin.campaigns.index') }}" class="text-sm font-semibold text-indigo-600">← Campaign</a><h2 class="mt-3 text-2xl font-bold text-slate-950">{{ $campaign->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $campaign->brand->name }} · {{ $campaign->start_date?->format('d M Y') ?: '-' }} — {{ $campaign->end_date?->format('d M Y') ?: '-' }}</p></div><div class="flex gap-2"><x-dashboard.status-badge :status="$campaign->status"/><a href="{{ route('superadmin.campaigns.edit',$campaign) }}" class="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Edit</a></div></div><div class="grid gap-6 lg:grid-cols-[1fr_1.3fr]"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold text-slate-950">Brief campaign</h3><dl class="mt-5 space-y-5 text-sm"><div><dt class="text-xs text-slate-400">Budget</dt><dd class="mt-1 font-semibold text-slate-700">Rp{{ number_format($campaign->budget,0,',','.') }}</dd></div><div><dt class="text-xs text-slate-400">Deskripsi</dt><dd class="mt-1 leading-6 text-slate-600">{{ $campaign->description ?: '-' }}</dd></div><div><dt class="text-xs text-slate-400">Content requirement</dt><dd class="mt-1 whitespace-pre-line leading-6 text-slate-600">{{ $campaign->content_requirements ?: '-' }}</dd></div><div><dt class="text-xs text-slate-400">Do & don'ts</dt><dd class="mt-1 whitespace-pre-line leading-6 text-slate-600">{{ $campaign->dos_and_donts ?: '-' }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between"><h3 class="font-bold text-slate-950">Assignment KOL</h3><span class="text-xs text-slate-400">{{ $campaign->endorsements->count() }} KOL</span></div><form method="POST" action="{{ route('superadmin.campaigns.assign',$campaign) }}" class="mt-4 grid gap-3 sm:grid-cols-2">@csrf<select name="kol_profile_id" required class="rounded-xl border-slate-200 text-sm"><option value="">Pilih KOL</option>@foreach($kols as $kol)<option value="{{ $kol->id }}">{{ $kol->user->name }}</option>@endforeach</select><select name="content_type" class="rounded-xl border-slate-200 text-sm"><option value="reels">Reels</option><option value="video">Video</option><option value="feed_post">Feed post</option><option value="story">Story</option></select><input type="number" name="fee" min="0" required placeholder="Fee (Rp)" class="rounded-xl border-slate-200 text-sm"><input type="date" name="deadline" required class="rounded-xl border-slate-200 text-sm"><button class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white sm:col-span-2">Assign KOL</button></form><div class="mt-6 space-y-3">@forelse($campaign->endorsements as $endorsement)<div class="flex items-center justify-between rounded-xl bg-slate-50 p-3"><div><p class="text-sm font-semibold text-slate-800">{{ $endorsement->kolProfile->user->name }}</p><p class="text-xs text-slate-400">{{ str($endorsement->content_type)->replace('_',' ')->title() }} · Rp{{ number_format($endorsement->fee,0,',','.') }}</p></div><x-dashboard.status-badge :status="$endorsement->status"/></div>@empty<p class="py-8 text-center text-sm text-slate-400">Belum ada KOL yang di-assign.</p>@endforelse</div></section></div>@endsection
