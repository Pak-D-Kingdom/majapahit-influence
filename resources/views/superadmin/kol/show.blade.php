@extends('superadmin.layouts.app')
@section('title', $kol->user->name)
@section('page-title', 'Detail KOL')
@section('content')
<div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('superadmin.kol.index') }}" class="text-sm font-semibold text-indigo-600"><i class="bi bi-arrow-left mr-1"></i>Data KOL</a><div class="mt-4 flex items-center gap-4"><div class="flex size-14 items-center justify-center rounded-2xl bg-indigo-100 text-xl font-bold text-indigo-700">{{ str($kol->user->name)->substr(0, 1)->upper() }}</div><div><h2 class="text-2xl font-bold tracking-tight text-slate-950">{{ $kol->user->name }}</h2><p class="text-sm text-slate-500">{{ $kol->nickname ?: 'Belum ada nama panggilan' }} · {{ $kol->city ?: 'Lokasi belum diisi' }}</p></div></div></div><div class="flex gap-2"><x-dashboard.status-badge :status="$kol->status"/><a href="{{ route('superadmin.kol.edit', $kol) }}" class="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800"><i class="bi bi-pencil"></i>Edit profil</a></div></div>
<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><x-dashboard.stat-card label="Tier" :value="$kol->tier?->name ?: '-'" icon="bi-award" hint="Klasifikasi KOL" accent="indigo"/><x-dashboard.stat-card label="Followers utama" :value="number_format($kol->socialMedia->max('followers_count') ?? 0)" icon="bi-people" hint="Data terakhir" accent="blue"/><x-dashboard.stat-card label="Engagement rate" :value="number_format($kol->socialMedia->max('engagement_rate') ?? 0, 2).'%'" icon="bi-graph-up" hint="Platform utama" accent="emerald"/><x-dashboard.stat-card label="Endorsement" :value="number_format($kol->endorsements->count())" icon="bi-briefcase" hint="Total assignment" accent="amber"/></div>
<div class="mt-6 grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2"><h3 class="font-bold text-slate-950">Informasi profil</h3><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs font-medium text-slate-400">Email</dt><dd class="mt-1 text-sm text-slate-700">{{ $kol->user->email }}</dd></div><div><dt class="text-xs font-medium text-slate-400">Domisili</dt><dd class="mt-1 text-sm text-slate-700">{{ collect([$kol->city, $kol->province])->filter()->join(', ') ?: '-' }}</dd></div><div class="sm:col-span-2"><dt class="text-xs font-medium text-slate-400">Niche</dt><dd class="mt-2 flex flex-wrap gap-2">@forelse ($kol->niches as $niche)<span class="rounded-lg bg-indigo-50 px-2.5 py-1 text-xs font-semibold text-indigo-700">{{ $niche->name }}</span>@empty<span class="text-sm text-slate-500">Belum ada niche</span>@endforelse</dd></div><div class="sm:col-span-2"><dt class="text-xs font-medium text-slate-400">Bio</dt><dd class="mt-1 text-sm leading-6 text-slate-600">{{ $kol->bio ?: 'Belum ada bio.' }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold text-slate-950">Social media</h3><div class="mt-5 space-y-4">@forelse ($kol->socialMedia as $social)<div class="flex items-center justify-between gap-3"><div><p class="text-sm font-semibold text-slate-800">{{ str($social->platform)->title() }}</p><p class="text-xs text-slate-400">{{ $social->username }}</p></div><div class="text-right"><p class="text-sm font-semibold text-slate-700">{{ number_format($social->followers_count) }}</p><p class="text-xs text-slate-400">{{ number_format($social->engagement_rate, 2) }}%</p></div></div>@empty<p class="text-sm text-slate-400">Belum ada social media.</p>@endforelse</div></section></div>
<div class="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between"><h3 class="font-bold text-slate-950">Riwayat endorsement</h3><span class="text-xs text-slate-400">{{ $kol->endorsements->count() }} assignment</span></div>@if ($kol->endorsements->isEmpty())<p class="py-12 text-center text-sm text-slate-400">Belum ada riwayat endorsement.</p>@else<div class="mt-5 overflow-x-auto"><table class="w-full min-w-[700px] text-left text-sm"><thead class="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-400"><tr><th class="px-4 py-3 font-semibold">Campaign</th><th class="px-4 py-3 font-semibold">Brand</th><th class="px-4 py-3 font-semibold">Fee</th><th class="px-4 py-3 font-semibold">Deadline</th><th class="px-4 py-3 font-semibold">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach ($kol->endorsements->sortByDesc('created_at') as $endorsement)<tr><td class="px-4 py-3 font-semibold text-slate-800">{{ $endorsement->campaign->name }}</td><td class="px-4 py-3 text-slate-500">{{ $endorsement->campaign->brand->name }}</td><td class="px-4 py-3 text-slate-600">Rp{{ number_format($endorsement->fee, 0, ',', '.') }}</td><td class="px-4 py-3 text-slate-500">{{ $endorsement->deadline->format('d M Y') }}</td><td class="px-4 py-3"><x-dashboard.status-badge :status="$endorsement->status"/></td></tr>@endforeach</tbody></table></div>@endif</div>
@endsection
