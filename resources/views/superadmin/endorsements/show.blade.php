@extends('superadmin.layouts.app')
@section('title','Detail Endorsement')
@section('page-title','Detail Endorsement')
@section('content')
<div class="mb-6 flex items-end justify-between gap-4"><div><a href="{{ route('superadmin.endorsements.index') }}" class="text-sm font-semibold text-indigo-600">← Endorsement</a><h2 class="mt-3 text-2xl font-bold text-slate-950">{{ $endorsement->campaign->name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $endorsement->campaign->brand->name }} · {{ $endorsement->kolProfile->user->name }}</p></div><a href="{{ route('superadmin.endorsements.edit',$endorsement) }}" class="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Edit</a></div>
<div class="grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2"><h3 class="font-bold">Detail pekerjaan</h3><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs text-slate-400">KOL</dt><dd class="mt-1 font-semibold">{{ $endorsement->kolProfile->user->name }}</dd></div><div><dt class="text-xs text-slate-400">Tipe konten</dt><dd class="mt-1">{{ str($endorsement->content_type)->replace('_',' ')->title() }}</dd></div><div><dt class="text-xs text-slate-400">Fee</dt><dd class="mt-1">Rp{{ number_format($endorsement->fee,0,',','.') }}</dd></div><div><dt class="text-xs text-slate-400">Deadline</dt><dd class="mt-1">{{ $endorsement->deadline->format('d M Y') }}</dd></div><div class="sm:col-span-2"><dt class="text-xs text-slate-400">Brief</dt><dd class="mt-1 whitespace-pre-line text-sm leading-6 text-slate-600">{{ $endorsement->campaign->content_requirements ?: '-' }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold">Content proof</h3>@forelse($endorsement->contentProofs as $proof)<div class="mt-4 rounded-xl bg-slate-50 p-4"><x-dashboard.status-badge :status="$proof->review_status"/>@if($proof->post_url)<a href="{{ $proof->post_url }}" target="_blank" rel="noopener" class="mt-3 block truncate text-sm font-semibold text-indigo-600">{{ $proof->post_url }}</a>@endif@if($proof->review_status === 'pending')<form method="POST" action="{{ route('superadmin.endorsements.proof.review',[$endorsement,$proof]) }}" class="mt-4 border-t border-slate-200 pt-4">@csrf<textarea name="review_notes" placeholder="Catatan jika ditolak..." class="w-full rounded-lg border-slate-200 text-xs"></textarea><div class="mt-2 flex gap-2"><button name="action" value="approve" class="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-semibold text-white">Approve</button><button name="action" value="reject" class="rounded-lg bg-rose-600 px-3 py-2 text-xs font-semibold text-white">Reject</button></div></form>@endif</div>@empty<p class="mt-5 text-sm text-slate-400">Belum ada content proof.</p>@endforelse</section></div>
@endsection
