@extends('superadmin.layouts.app')
@section('title','Edit Endorsement')
@section('page-title','Edit Endorsement')
@section('content')<div class="mb-6"><a href="{{ route('superadmin.endorsements.show',$endorsement) }}" class="text-sm font-semibold text-indigo-600">← Kembali ke detail</a><h2 class="mt-3 text-2xl font-bold text-slate-950">Edit endorsement</h2></div><form method="POST" action="{{ route('superadmin.endorsements.update',$endorsement) }}" class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">@csrf @method('PUT')<div class="grid gap-5 sm:grid-cols-2"><label class="text-sm font-medium text-slate-700">Tipe konten<select name="content_type" class="mt-2 w-full rounded-xl border-slate-200 text-sm">@foreach(['reels'=>'Reels','video'=>'Video','feed_post'=>'Feed post','story'=>'Story'] as $v=>$l)<option value="{{ $v }}" @selected($endorsement->content_type===$v)>{{ $l }}</option>@endforeach</select></label><label class="text-sm font-medium text-slate-700">Status<select name="status" class="mt-2 w-full rounded-xl border-slate-200 text-sm">@foreach(['assigned','in_progress','content_submitted','content_approved','content_rejected','selesai'] as $status)<option value="{{ $status }}" @selected($endorsement->status===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select></label><label class="text-sm font-medium text-slate-700">Fee<input type="number" name="fee" min="0" value="{{ $endorsement->fee }}" required class="mt-2 w-full rounded-xl border-slate-200 text-sm"></label><label class="text-sm font-medium text-slate-700">Deadline<input type="date" name="deadline" value="{{ $endorsement->deadline->format('Y-m-d') }}" required class="mt-2 w-full rounded-xl border-slate-200 text-sm"></label><label class="text-sm font-medium text-slate-700">Tanggal mulai<input type="date" name="start_date" value="{{ $endorsement->start_date?->format('Y-m-d') }}" class="mt-2 w-full rounded-xl border-slate-200 text-sm"></label><label class="text-sm font-medium text-slate-700 sm:col-span-2">Catatan<textarea name="notes" rows="4" class="mt-2 w-full rounded-xl border-slate-200 text-sm">{{ $endorsement->notes }}</textarea></label></div><div class="mt-6 flex justify-end gap-3"><a href="{{ route('superadmin.endorsements.show',$endorsement) }}" class="rounded-xl px-4 py-2.5 text-sm font-semibold text-slate-500">Batal</a><button class="rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-semibold text-white">Simpan perubahan</button></div></form>@endsection
