@extends('superadmin.layouts.app')
@section('title', 'Brand & Klien')
@section('page-title', 'Brand & Klien')
@section('content')
<div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm text-slate-500">Kelola brand dan klien yang bekerja sama dengan agensi.</p><h2 class="mt-1 text-2xl font-bold tracking-tight text-slate-950">Brand & Klien</h2></div><a href="{{ route('superadmin.brands.create') }}" class="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800"><i class="bi bi-plus-lg mr-1"></i>Tambah brand</a></div>
<form method="GET" class="mb-5 flex flex-wrap gap-2 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><label class="relative"><i class="bi bi-search absolute left-3 top-2.5 text-slate-400"></i><input name="search" value="{{ request('search') }}" placeholder="Cari brand..." class="rounded-xl border-slate-200 py-2 pl-9 text-sm"></label><select name="status" class="rounded-xl border-slate-200 text-sm"><option value="">Semua status</option><option value="active" @selected(request('status') === 'active')>Aktif</option><option value="inactive" @selected(request('status') === 'inactive')>Nonaktif</option></select><button class="rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Filter</button><a href="{{ route('superadmin.brands.index') }}" class="rounded-xl px-4 py-2 text-sm font-semibold text-slate-500 hover:bg-slate-100">Reset</a></form>
<div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($brands as $brand)<a href="{{ route('superadmin.brands.show',$brand) }}" class="group rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-indigo-200 hover:shadow-md"><div class="flex items-start justify-between gap-3"><div class="flex size-12 items-center justify-center rounded-xl bg-indigo-50 text-lg font-bold text-indigo-600">{{ str($brand->name)->substr(0,1)->upper() }}</div><x-dashboard.status-badge :status="$brand->is_active ? 'aktif' : 'nonaktif'"/></div><h3 class="mt-5 font-bold text-slate-950 group-hover:text-indigo-600">{{ $brand->name }}</h3><p class="mt-1 text-sm text-slate-500">{{ $brand->industry ?: 'Industri belum diisi' }}</p><div class="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-xs text-slate-400"><span><i class="bi bi-megaphone mr-1"></i>{{ $brand->campaigns_count }} campaign</span><span>Lihat detail <i class="bi bi-arrow-up-right ml-1"></i></span></div></a>@empty<div class="col-span-full rounded-2xl border border-dashed border-slate-300 bg-white py-16 text-center text-sm text-slate-400"><i class="bi bi-building mb-2 block text-3xl"></i>Belum ada brand.</div>@endforelse</div><div class="mt-6">{{ $brands->links() }}</div>
@endsection
