@extends('superadmin.layouts.app')
@section('title',$brand->name)
@section('page-title','Detail Brand')
@section('content')
<div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('superadmin.brands.index') }}" class="text-sm font-semibold text-indigo-600"><i class="bi bi-arrow-left mr-1"></i>Brand & Klien</a><div class="mt-4 flex items-center gap-4"><div class="flex size-14 items-center justify-center rounded-2xl bg-indigo-100 text-xl font-bold text-indigo-700">{{ str($brand->name)->substr(0,1)->upper() }}</div><div><h2 class="text-2xl font-bold text-slate-950">{{ $brand->name }}</h2><p class="text-sm text-slate-500">{{ $brand->industry ?: 'Industri belum diisi' }}</p></div></div></div><div class="flex gap-2"><x-dashboard.status-badge :status="$brand->is_active ? 'aktif' : 'nonaktif'"/><a href="{{ route('superadmin.brands.edit',$brand) }}" class="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Edit</a></div></div><div class="grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2"><h3 class="font-bold text-slate-950">Informasi kontak</h3><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs text-slate-400">Nama PIC</dt><dd class="mt-1 text-sm text-slate-700">{{ $brand->pic_name ?: '-' }}</dd></div><div><dt class="text-xs text-slate-400">Jabatan</dt><dd class="mt-1 text-sm text-slate-700">{{ $brand->pic_title ?: '-' }}</dd></div><div><dt class="text-xs text-slate-400">Email</dt><dd class="mt-1 text-sm text-slate-700">{{ $brand->pic_email ?: '-' }}</dd></div><div><dt class="text-xs text-slate-400">Telepon</dt><dd class="mt-1 text-sm text-slate-700">{{ $brand->pic_phone ?: '-' }}</dd></div><div class="sm:col-span-2"><dt class="text-xs text-slate-400">Alamat</dt><dd class="mt-1 text-sm leading-6 text-slate-600">{{ $brand->address ?: '-' }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold text-slate-950">Ringkasan</h3><p class="mt-5 text-3xl font-bold text-slate-950">{{ $brand->campaigns->count() }}</p><p class="text-sm text-slate-500">Total campaign</p></section></div><section class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="p-5"><h3 class="font-bold text-slate-950">Campaign terkait</h3></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-400"><tr><th class="px-5 py-3">Nama campaign</th><th class="px-5 py-3">Periode</th><th class="px-5 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($brand->campaigns as $campaign)<tr><td class="px-5 py-4 font-semibold text-slate-800">{{ $campaign->name }}</td><td class="px-5 py-4 text-slate-500">{{ $campaign->start_date?->format('d M Y') ?: '-' }} — {{ $campaign->end_date?->format('d M Y') ?: '-' }}</td><td class="px-5 py-4"><x-dashboard.status-badge :status="$campaign->status"/></td></tr>@empty<tr><td colspan="3" class="px-5 py-12 text-center text-sm text-slate-400">Belum ada campaign untuk brand ini.</td></tr>@endforelse</tbody></table></div></section>
@endsection
