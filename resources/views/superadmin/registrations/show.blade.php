@extends('superadmin.layouts.app')
@section('title', 'Review Pendaftaran')
@section('page-title', 'Review Pendaftaran')
@section('content')<div class="mb-6"><a href="{{ route('superadmin.registrations.index') }}" class="text-sm font-semibold text-indigo-600"><i class="bi bi-arrow-left mr-1"></i>Kembali ke pendaftaran</a><div class="mt-4 flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p class="font-mono text-xs text-indigo-600">{{ $registration->registration_number }}</p><h2 class="mt-1 text-2xl font-bold text-slate-950">{{ $registration->full_name }}</h2><p class="mt-1 text-sm text-slate-500">{{ $registration->email }} · {{ $registration->phone }}</p></div><x-dashboard.status-badge :status="$registration->status"/></div></div><div class="grid gap-6 lg:grid-cols-[1.2fr_0.8fr]"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold text-slate-950">Detail pendaftar</h3><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs text-slate-400">Kota</dt><dd class="mt-1 text-sm text-slate-700">{{ $registration->city ?: '-' }}</dd></div><div><dt class="text-xs text-slate-400">Platform</dt><dd class="mt-1 text-sm text-slate-700">{{ str(data_get($registration->social_media, 'platform', '-'))->title() }} · {{ data_get($registration->social_media, 'username', '-') }}</dd></div><div><dt class="text-xs text-slate-400">Followers</dt><dd class="mt-1 text-sm text-slate-700">{{ number_format(data_get($registration->social_media, 'followers_count', 0)) }}</dd></div><div><dt class="text-xs text-slate-400">Niche</dt><dd class="mt-1 text-sm text-slate-700">{{ collect($registration->niches)->join(', ') ?: '-' }}</dd></div><div class="sm:col-span-2"><dt class="text-xs text-slate-400">Alasan bergabung</dt><dd class="mt-1 text-sm leading-6 text-slate-600">{{ $registration->join_reason ?: '-' }}</dd></div><div class="sm:col-span-2"><dt class="text-xs text-slate-400">Rate card</dt><dd class="mt-1 text-sm leading-6 text-slate-600">{{ $registration->expected_rate ?: '-' }}</dd></div></dl><h3 class="mt-8 font-bold text-slate-950">File portofolio</h3><div class="mt-3 space-y-2">@forelse ($registration->files as $file)<div class="flex items-center gap-3 rounded-xl bg-slate-50 p-3 text-sm"><i class="bi bi-file-earmark text-indigo-600"></i><span class="truncate">{{ $file->file_name }}</span><span class="ml-auto text-xs text-slate-400">{{ number_format($file->file_size / 1024, 0) }} KB</span></div>@empty<p class="text-sm text-slate-400">Tidak ada file.</p>@endforelse</div></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 class="font-bold text-slate-950">Keputusan review</h3>@if ($registration->status === 'pending_review')<form method="POST" action="{{ route('superadmin.registrations.review', $registration) }}" class="mt-5 space-y-4">@csrf<label class="text-sm font-medium text-slate-700">Catatan internal<textarea name="notes" rows="4" class="mt-2 w-full rounded-xl border-slate-200 text-sm"></textarea></label><div class="grid gap-3"><button name="action" value="approve" class="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-emerald-700"><i class="bi bi-check2 mr-1"></i>Approve pendaftaran</button><button type="button" id="show-reject" class="rounded-xl border border-rose-200 px-4 py-2.5 text-sm font-semibold text-rose-600 hover:bg-rose-50">Reject pendaftaran</button></div><div id="reject-box" class="hidden border-t border-slate-100 pt-4"><label class="text-sm font-medium text-slate-700">Alasan penolakan<textarea name="rejection_reason" rows="3" class="mt-2 w-full rounded-xl border-slate-200 text-sm"></textarea></label><button name="action" value="reject" class="mt-3 w-full rounded-xl bg-rose-600 px-4 py-2.5 text-sm font-semibold text-white">Konfirmasi reject</button></div></form>@else<p class="mt-4 text-sm leading-6 text-slate-500">Pendaftaran ini sudah diproses. Data keputusan tersimpan di audit trail.</p>@endif</section></div>@push('scripts')<script>document.getElementById('show-reject')?.addEventListener('click',()=>document.getElementById('reject-box').classList.remove('hidden'));</script>@endpush@endsection
